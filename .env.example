# Anthropic API 키 (자연어 입력 모듈의 LLM 해석에 사용)
# - 이 값은 Vercel 대시보드의 Settings > Environment Variables 에만 입력합니다.
# - 이 파일(.env.example)에는 실제 키를 적지 마십시오. 커밋되면 키가 노출됩니다.
# - 키가 없어도 데모는 규칙 기반 폴백 파서로 자연어 입력이 동작합니다.
ANTHROPIC_API_KEY=
